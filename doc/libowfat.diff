diff -Naur libowfat-0.20/dns/dns_dfd.c libowfat-0.20-eike/dns/dns_dfd.c
--- libowfat-0.20/dns/dns_dfd.c	2003-09-12 13:35:16.000000000 +0200
+++ libowfat-0.20-eike/dns/dns_dfd.c	2004-09-17 14:11:19.211853448 +0200
@@ -60,7 +60,7 @@
   name[namelen++] = 0;
 
   x = malloc(namelen);
-  if (!x) return 0;
+  if (!x) { errno = ENOMEM; return 0; }
   byte_copy(x,namelen,name);
 
   if (*out) free(*out);
diff -Naur libowfat-0.20/dns/dns_ip6.3 libowfat-0.20-eike/dns/dns_ip6.3
--- libowfat-0.20/dns/dns_ip6.3	2002-09-17 03:00:11.000000000 +0200
+++ libowfat-0.20-eike/dns/dns_ip6.3	2004-09-17 14:11:19.207854056 +0200
@@ -1,6 +1,6 @@
 .TH dns_ip6 3
 .SH NAME
-dns_ip6 \- look up IPv4 addresses
+dns_ip6 \- look up IPv6 addresses
 .SH SYNTAX
 .B #include <dns.h>
 
diff -Naur libowfat-0.20/dns/dns_ip6.c libowfat-0.20-eike/dns/dns_ip6.c
--- libowfat-0.20/dns/dns_ip6.c	2002-09-17 02:56:56.000000000 +0200
+++ libowfat-0.20-eike/dns/dns_ip6.c	2004-09-17 14:11:19.212853296 +0200
@@ -4,6 +4,7 @@
 #include "dns.h"
 #include "ip4.h"
 #include "ip6.h"
+#include "str.h"
 
 static int dns_ip6_packet_add(stralloc *out,const char *buf,unsigned int len)
 {
@@ -48,25 +49,22 @@
 
 static char *q = 0;
 
-int dns_ip6(stralloc *out,stralloc *fqdn)
+static int dns_ip6q(stralloc *out, const char *fqdn, const unsigned int len)
 {
   unsigned int i;
   char code;
   char ch;
   char ip[16];
 
-  if (!stralloc_copys(out,"")) return -1;
-  if (!stralloc_readyplus(fqdn,1)) return -1;
-  fqdn->s[fqdn->len]=0;
-  if ((i=scan_ip6(fqdn->s,ip))) {
-    if (fqdn->s[i]) return -1;
+  if ((i=scan_ip6(fqdn,ip))) {
+    if (fqdn[i]) return -1;
     stralloc_copyb(out,ip,16);
     return 0;
   }
   code = 0;
-  for (i = 0;i <= fqdn->len;++i) {
-    if (i < fqdn->len)
-      ch = fqdn->s[i];
+  for (i = 0;i <= len;++i) {
+    if (i < len)
+      ch = fqdn[i];
     else
       ch = '.';
 
@@ -82,14 +80,14 @@
       continue;
     }
 
-    if (!dns_domain_fromdot(&q,fqdn->s,fqdn->len)) return -1;
+    if (!dns_domain_fromdot(&q,fqdn,len)) return -1;
     if (!stralloc_copys(out,"")) return -1;
     if (dns_resolve(q,DNS_T_AAAA) != -1)
       if (dns_ip6_packet_add(out,dns_resolve_tx.packet,dns_resolve_tx.packetlen) != -1) {
 	dns_transmit_free(&dns_resolve_tx);
 	dns_domain_free(&q);
       }
-    if (!dns_domain_fromdot(&q,fqdn->s,fqdn->len)) return -1;
+    if (!dns_domain_fromdot(&q,fqdn,len)) return -1;
     if (dns_resolve(q,DNS_T_A) != -1)
       if (dns_ip6_packet_add(out,dns_resolve_tx.packet,dns_resolve_tx.packetlen) != -1) {
 	dns_transmit_free(&dns_resolve_tx);
@@ -101,3 +99,23 @@
   out->len &= ~3;
   return 0;
 }
+
+int dns_ip6(stralloc *out,stralloc *fqdn)
+{
+  if (!stralloc_copys(out,"")) return -1;
+  if (!stralloc_readyplus(fqdn,1)) return -1;
+  fqdn->s[fqdn->len]=0;
+
+  return dns_ip6q(out, fqdn->s, fqdn->len);
+}
+
+int dnsip6(char **out, unsigned int *len, const char *fqdn)
+{
+  stralloc sa;
+  int r;
+
+  r = dns_ip6q(&sa, fqdn, str_len(fqdn));
+  *out = sa.s;
+  *len = sa.len;
+  return r;
+}
diff -Naur libowfat-0.20/dns/dns_ip.c libowfat-0.20-eike/dns/dns_ip.c
--- libowfat-0.20/dns/dns_ip.c	2001-02-11 22:11:45.000000000 +0100
+++ libowfat-0.20-eike/dns/dns_ip.c	2004-09-17 14:11:19.213853144 +0200
@@ -2,6 +2,7 @@
 #include "uint16.h"
 #include "byte.h"
 #include "dns.h"
+#include "str.h"
 
 int dns_ip4_packet(stralloc *out,const char *buf,unsigned int len)
 {
@@ -36,7 +37,7 @@
 
 static char *q = 0;
 
-int dns_ip4(stralloc *out,const stralloc *fqdn)
+static int dns_ip4q(stralloc *out,const char *fqdn, const unsigned int len)
 {
   unsigned int i;
   char code;
@@ -44,9 +45,9 @@
 
   if (!stralloc_copys(out,"")) return -1;
   code = 0;
-  for (i = 0;i <= fqdn->len;++i) {
-    if (i < fqdn->len)
-      ch = fqdn->s[i];
+  for (i = 0;i <= len;++i) {
+    if (i < len)
+      ch = fqdn[i];
     else
       ch = '.';
 
@@ -62,7 +63,7 @@
       continue;
     }
 
-    if (!dns_domain_fromdot(&q,fqdn->s,fqdn->len)) return -1;
+    if (!dns_domain_fromdot(&q,fqdn,len)) return -1;
     if (dns_resolve(q,DNS_T_A) == -1) return -1;
     if (dns_ip4_packet(out,dns_resolve_tx.packet,dns_resolve_tx.packetlen) == -1) return -1;
     dns_transmit_free(&dns_resolve_tx);
@@ -73,3 +74,19 @@
   out->len &= ~3;
   return 0;
 }
+
+int dns_ip4(stralloc *out,const stralloc *fqdn)
+{
+  return dns_ip4q(out, fqdn->s, fqdn->len);
+}
+
+int dnsip4(char **out, unsigned int *len, const char *fqdn)
+{
+  stralloc sa;
+  int r;
+
+  r = dns_ip4q(&sa, fqdn, str_len(fqdn));
+  *out = sa.s;
+  *len = sa.len;
+  return r;
+}
diff -Naur libowfat-0.20/dns/dns_mx.c libowfat-0.20-eike/dns/dns_mx.c
--- libowfat-0.20/dns/dns_mx.c	2001-02-11 22:11:45.000000000 +0100
+++ libowfat-0.20-eike/dns/dns_mx.c	2004-09-17 14:11:19.208853904 +0200
@@ -2,6 +2,7 @@
 #include "byte.h"
 #include "uint16.h"
 #include "dns.h"
+#include "str.h"
 
 static char *q = 0;
 
@@ -47,3 +48,17 @@
   dns_domain_free(&q);
   return 0;
 }
+
+int dnsmx(char **out,unsigned int *len,const char *fqdn)
+{
+  stralloc sa;
+
+  if (!dns_domain_fromdot(&q,fqdn,str_len(fqdn))) return -1;
+  if (dns_resolve(q,DNS_T_MX) == -1) return -1;
+  if (dns_mx_packet(&sa,dns_resolve_tx.packet,dns_resolve_tx.packetlen) == -1) return -1;
+  dns_transmit_free(&dns_resolve_tx);
+  dns_domain_free(&q);
+  *out = sa.s;
+  *len = sa.len;
+  return 0;
+}
diff -Naur libowfat-0.20/dns/dns_name6_domain.3 libowfat-0.20-eike/dns/dns_name6_domain.3
--- libowfat-0.20/dns/dns_name6_domain.3	2004-07-13 12:09:14.000000000 +0200
+++ libowfat-0.20-eike/dns/dns_name6_domain.3	2004-09-17 14:11:19.207854056 +0200
@@ -18,6 +18,6 @@
 
 q is zero terminated.
 
-q must have space for DNS_NAME4_DOMAIN bytes.
+q must have space for DNS_NAME6_DOMAIN bytes.
 .SH "SEE ALSO"
 dns_name6(3), dns_name4_domain(3)
diff -Naur libowfat-0.20/dns/dns_txt.c libowfat-0.20-eike/dns/dns_txt.c
--- libowfat-0.20/dns/dns_txt.c	2001-02-11 22:11:45.000000000 +0100
+++ libowfat-0.20-eike/dns/dns_txt.c	2004-09-17 14:11:19.216852688 +0200
@@ -1,7 +1,9 @@
+#include <errno.h>
 #include "stralloc.h"
 #include "uint16.h"
 #include "byte.h"
 #include "dns.h"
+#include "str.h"
 
 int dns_txt_packet(stralloc *out,const char *buf,unsigned int len)
 {
@@ -26,7 +28,7 @@
     uint16_unpack_big(header + 8,&datalen);
     if (byte_equal(header,2,DNS_T_TXT))
       if (byte_equal(header + 2,2,DNS_C_IN)) {
-	if (pos + datalen > len) return -1;
+	if (pos + datalen > len) { errno = EINVAL; return -1; }
 	txtlen = 0;
 	for (i = 0;i < datalen;++i) {
 	  ch = buf[pos + i];
@@ -57,3 +59,16 @@
   dns_domain_free(&q);
   return 0;
 }
+
+int dnstxt(char **out,const char *fqdn)
+{
+  stralloc sa;
+
+  if (!dns_domain_fromdot(&q,fqdn,str_len(fqdn))) return -1;
+  if (dns_resolve(q,DNS_T_TXT) == -1) return -1;
+  if (dns_txt_packet(&sa,dns_resolve_tx.packet,dns_resolve_tx.packetlen) == -1) return -1;
+  dns_transmit_free(&dns_resolve_tx);
+  dns_domain_free(&q);
+  *out = (sa.len > 1) ? sa.s : 0;
+  return 0;
+}
diff -Naur libowfat-0.20/dns/dns_txt_packet.3 libowfat-0.20-eike/dns/dns_txt_packet.3
--- libowfat-0.20/dns/dns_txt_packet.3	2002-09-17 03:25:37.000000000 +0200
+++ libowfat-0.20-eike/dns/dns_txt_packet.3	2004-09-17 14:11:19.207854056 +0200
@@ -7,9 +7,9 @@
 int \fBdns_txt_packet\fP(stralloc* \fIout\fR,const char* \fIbuf\fR,
                   unsigned int \fIlen\fR);
 .SH DESCRIPTION
-dns_txt_packet is a low-level component of dns_mx, designed to support
+dns_txt_packet is a low-level component of dns_txt, designed to support
 asynchronous DNS lookups. It reads a DNS packet of length \fIlen\fR from \fIbuf\fR,
 extracts the TXT records from the answer section of the packet, puts the
-result into \fIout\fR, and returns 0 or -1 the same way as dns_mx.
+result into \fIout\fR, and returns 0 or -1 the same way as dns_txt.
 .SH "SEE ALSO"
-dns_mx(3)
+dns_txt(3)
diff -Naur libowfat-0.20/dns.h libowfat-0.20-eike/dns.h
--- libowfat-0.20/dns.h	2004-07-21 17:40:08.000000000 +0200
+++ libowfat-0.20-eike/dns.h	2004-09-17 14:11:19.253847064 +0200
@@ -70,16 +70,20 @@
 
 int dns_ip4_packet(stralloc *,const char *,unsigned int);
 int dns_ip4(stralloc *,const stralloc *);
+int dnsip4(char **, unsigned int *, const char *);
 int dns_ip6_packet(stralloc *,const char *,unsigned int);
 int dns_ip6(stralloc *,stralloc *);
+int dnsip6(char **, unsigned int *, const char *);
 int dns_name_packet(stralloc *,const char *,unsigned int);
 void dns_name4_domain(char *,const char *);
 #define DNS_NAME4_DOMAIN 31
 int dns_name4(stralloc *,const char *);
 int dns_txt_packet(stralloc *,const char *,unsigned int);
 int dns_txt(stralloc *,const stralloc *);
+int dnstxt(char **,const char *);
 int dns_mx_packet(stralloc *,const char *,unsigned int);
 int dns_mx(stralloc *,const stralloc *);
+int dnsmx(char **, unsigned int *, const char *);
 
 int dns_resolvconfrewrite(stralloc *);
 int dns_ip4_qualify_rules(stralloc *,stralloc *,const stralloc *,const stralloc *);
diff -Naur libowfat-0.20/socket/fmt_ip4.3 libowfat-0.20-eike/socket/fmt_ip4.3
--- libowfat-0.20/socket/fmt_ip4.3	2001-02-05 23:05:34.000000000 +0100
+++ libowfat-0.20-eike/socket/fmt_ip4.3	2004-09-17 14:11:19.113868344 +0200
@@ -24,4 +24,4 @@
   char ip[4];
   buf[fmt_ip4(buf,ip)]=0;
 .SH "SEE ALSO"
-scan_ip4(3), fmt_ip6(3)
+scan_ip4(3), fmt_ip6(3), inet_ntop(3)
diff -Naur libowfat-0.20/socket/fmt_ip6.3 libowfat-0.20-eike/socket/fmt_ip6.3
--- libowfat-0.20/socket/fmt_ip6.3	2003-12-05 20:40:21.000000000 +0100
+++ libowfat-0.20-eike/socket/fmt_ip6.3	2004-09-17 14:11:19.078873664 +0200
@@ -29,4 +29,4 @@
   char ip[16];
   buf[fmt_ip6(buf,ip)]=0;
 .SH "SEE ALSO"
-fmt_ip6c(3), fmt_ip6if(3), scan_ip6(3), fmt_ip4(3)
+fmt_ip6c(3), fmt_ip6if(3), scan_ip6(3), fmt_ip4(3), inet_ntop(3)
diff -Naur libowfat-0.20/socket/scan_ip4.3 libowfat-0.20-eike/socket/scan_ip4.3
--- libowfat-0.20/socket/scan_ip4.3	2001-02-05 23:04:51.000000000 +0100
+++ libowfat-0.20-eike/socket/scan_ip4.3	2004-09-17 14:11:19.119867432 +0200
@@ -23,4 +23,4 @@
     parse_error();
 
 .SH "SEE ALSO"
-fmt_ip4(3), scan_ip6(3)
+fmt_ip4(3), scan_ip6(3), inet_pton(3)
diff -Naur libowfat-0.20/socket/scan_ip6.3 libowfat-0.20-eike/socket/scan_ip6.3
--- libowfat-0.20/socket/scan_ip6.3	2001-02-05 23:05:09.000000000 +0100
+++ libowfat-0.20-eike/socket/scan_ip6.3	2004-09-17 14:11:19.026881568 +0200
@@ -32,4 +32,4 @@
     parse_error();
 
 .SH "SEE ALSO"
-fmt_ip6(3), scan_ip4(3)
+fmt_ip6(3), scan_ip4(3), inet_pton(3)
